package arrayAndStringProblem;

public class Ex3 {

    /*
        Design an algorithm and write code to remove the duplicate characters in a
        string without using any additional buffer.
        NOTE: One or two additional variables are fine.An extra on the array is not
        FOLLOW UP
        Write test cases for this method

        s1 verif daca text diferit de null
        s2 sort the text asc
        s3 for every
     */
    public static void main(String[] args) {




    }

    public static void duplicateCharacters(char[] str)
    {
        if(str!=null) {
            int length = str.length;
            if(length<2) return;

            

        }else {
            System.out.println("Text is null");
        }
    }
}
